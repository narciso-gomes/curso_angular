import { RemoveAccentsDirective } from './remove-accents.directive';

describe('RemoveAccentsDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveAccentsDirective();
    expect(directive).toBeTruthy();
  });
});
